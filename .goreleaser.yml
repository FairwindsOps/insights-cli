## DO NOT EDIT - Managed by Terraform
# yaml-language-server: $$schema=https://goreleaser.com/static/schema.json

version: 2
project_name: insights-cli
brews:
  - name: insights
    skip_upload: auto
    goarm: 6
    repository:
      owner: FairwindsOps
      name: homebrew-tap
    directory: Formula
    description: Command Line utility for Fairwinds Insights
    test: |
      system "#{bin}/insights version"
builds:
  - id: insights-cli
    ldflags:
      - -X github.com/fairwindsops/insights-cli/pkg/version.version={{.Version}} -X github.com/fairwindsops/insights-cli/pkg/version.commit={{.Commit}} -s -w
    env:
      - CGO_ENABLED=0
    main: ./cmd/insights
    goarch:
      - 386
      - amd64
      - arm
      - arm64
    goos:
      - linux
      - darwin
      - windows
    goarm:
      - 6
      - 7
    ignore:
      - goos: windows
        goarch: arm64
      - goos: windows
        goarch: arm
release:
  disable: '{{ eq (envOrDefault "GORELEASER_SKIP_RELEASE" "false") "true" }}'
  prerelease: auto
  github:
    owner: FairwindsOps
    name: insights-cli
  footer: |
    You can verify the signatures of both the checksums.txt file and the published docker images using [cosign](https://github.com/sigstore/cosign).

    ```bash
    cosign verify-blob checksums.txt --bundle=checksums.txt.sigstore.json --key https://artifacts.fairwinds.com/cosign-p256.pub
    ```

    ```bash
    cosign verify us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .Tag }} --key https://artifacts.fairwinds.com/cosign-p256.pub
    ```
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
checksum:
  name_template: "checksums.txt"
signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - "sign-blob"
      - "--key=hashivault://cosign-p256"
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: all
docker_signs:
  - artifacts: all
    args: ["sign", "--key=hashivault://cosign-p256", "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli@${digest}", "-r", "--yes"]
dockers:
   - image_templates:
        - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .FullCommit }}-amd64"
        - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .Tag }}-amd64"
     use: buildx
     dockerfile: Dockerfile
     build_flag_templates:
        - "--platform=linux/amd64"
   - image_templates:
        - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .FullCommit }}-arm64v8"
        - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .Tag }}-arm64v8"
     use: buildx
     goarch: arm64
     goos: linux
     dockerfile: Dockerfile
     build_flag_templates:
        - "--platform=linux/arm64/v8"
   - image_templates:
        - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .FullCommit }}-armv7"
        - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .Tag }}-armv7"
     use: buildx
     goarch: arm
     goarm: 7
     goos: linux
     dockerfile: Dockerfile
     build_flag_templates:
        - "--platform=linux/arm/v7"
docker_manifests:
  - name_template: us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .Tag }}
    image_templates:
      - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .Tag }}-amd64"
      - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .Tag }}-arm64v8"
      - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .Tag }}-armv7"
  - name_template: us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .FullCommit }}
    image_templates:
      - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .FullCommit }}-amd64"
      - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .FullCommit }}-arm64v8"
      - "us-docker.pkg.dev/fairwinds-ops/oss/insights-cli:{{ .FullCommit }}-armv7"
